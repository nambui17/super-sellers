import React from 'react';
import {Box} from '@chakra-ui/react';
import { useMutation } from '@apollo/client';

export default function WishlistItem({imageUrl, artist, albumTitle, price}) {

    return (
        <Box>
            
        </Box>
    )
}